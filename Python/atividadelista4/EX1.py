def algaritmos_totais(x):
    numero_input = str(x)
    calculo = list(numero_input)
    resultado = len(calculo)
    return resultado

print(algaritmos_totais(int(input('Insira um número inteiro: '))))